#include <stdio.h>
/**
 * main - A program that will print a line using the printf function
 * Returns: 0 (success)
 */

int main(void)
{
       	printf("with proper grammar, but the outcome is a piece of art,n")
		return(0);
}
